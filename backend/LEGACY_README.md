# Legacy Node Backend

This directory contains the original Express/MongoDB implementation for project-history reference only. It is not used by the current application or Render deployment.

The production backend is the Spring Boot application in `spring-backend/`. It uses PostgreSQL and Spring Data JPA as required by the final-project specification.
